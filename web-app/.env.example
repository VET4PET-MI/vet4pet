# Base URL of the backend API.
# In development, omit this and the app falls back to http://localhost:5000.
# In production (Vercel), set this to your deployed backend URL, e.g.
#   VITE_API_URL=https://vet4pet-api.onrender.com
VITE_API_URL=
